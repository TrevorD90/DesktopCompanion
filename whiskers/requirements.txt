# Web app
flask>=3.0.0

# Anthropic Claude API (the only LLM backend)
anthropic>=0.30.0

# Speech recognition
RealtimeSTT>=0.3.0; python_version < "3.13"
faster-whisper>=1.0.0; python_version < "3.13"
openwakeword>=0.6.0

# Text to speech (ElevenLabs primary, Kokoro fallback, pyttsx3 second fallback)
elevenlabs>=1.0.0
kokoro>=0.7.0; python_version < "3.13"
sounddevice>=0.4.6
soundfile>=0.12.1
pyttsx3>=2.90

# Coursework upload
pypdf>=4.0.0
Pillow>=10.0.0

# Audio capture (wake word — requires C++ Build Tools on Windows)
PyAudio>=0.2.13; python_version < "3.13"

# Hotkey fallback
keyboard>=0.13.5

# Utilities
numpy>=1.24.0
requests>=2.31.0
