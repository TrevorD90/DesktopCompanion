# Core AI (local)
ollama>=0.3.0

# Cloud AI providers (optional — app defaults to local Ollama)
openai>=1.0.0
anthropic>=0.30.0

# Speech recognition (optional — app falls back to F9 hotkey if missing)
RealtimeSTT>=0.3.0; python_version < "3.13"
faster-whisper>=1.0.0; python_version < "3.13"
openwakeword>=0.6.0

# Text to speech (optional — app falls back to pyttsx3 if missing)
kokoro>=0.7.0; python_version < "3.13"
sounddevice>=0.4.6
soundfile>=0.12.1

# Character window
Pillow>=10.0.0
pywin32>=306

# Fallback TTS
pyttsx3>=2.90

# Utilities
numpy>=1.24.0
requests>=2.31.0

# Audio capture (for wake word — requires C++ Build Tools on Windows to compile)
PyAudio>=0.2.13; python_version < "3.13"

# Hotkey fallback
keyboard>=0.13.5

# System tray icon
pystray>=0.19.4
